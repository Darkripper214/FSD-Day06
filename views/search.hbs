<div class="container">
  <div class="d-flex flex-column align-items-center">

    <h2>Search Result</h2>

    {{#if result}}

    <h3>Showing Result from {{offset}} to {{math offset "+" resultLength}}</h3>
    <table class="table table-striped table-bordered">
      <thead class="thead-dark ">
        <tr>
          <th scope="col">App ID</th>
          <th scope="col">Name</th>
          <th scope="col">Rating</th>
          <th scope="col">Download</th>
          <th scope="col">Category</th>
        </tr>
      </thead>
      <tbody>
        {{#each result}}
        <tr>
          <td>{{app_id}}</td>
          <td>{{name}}</td>
          <td>{{rating}}</td>
          <td>{{installs}}</td>
          <td>{{category}}</td>
        </tr>
        {{/each }}
      </tbody>
    </table>
    {{else }}
    <h1>No result</h1>
    {{/if }}
    <div class="" style="width:100%">
      <div class="row">
        <div class="col">
          <form action="/search" method='get' class="d-flex justify-content-start">
            <input type="hidden" type="text" name="offset" placeholder="input search" value={{math offset "-" 10}}>
            <input type="hidden" type="text" name="q" placeholder="input search" value={{q}}>
            {{#if previousPageState}}
            <button class="btn btn-warning" type="submit">Previous Page</button>
            {{/if }}
          </form>
        </div>
        <div class="col d-flex justify-content-center">
          <a class="btn btn-primary" href="/" role="button">Return Home</a>
        </div>
        <div class="col">
          <form action="/search" method='get' class="d-flex justify-content-end">
            <input type="hidden" type="text" name="offset" placeholder="input search" value={{math offset "+" 10}}>
            <input type="hidden" type="text" name="q" placeholder="input search" value={{q}}>
            {{#if nextPageState}}
            <button class="btn btn-warning" type="submit">Next Page</button>
            {{/if }}
          </form>
        </div>
      </div>
    </div>


  </div>
</div>